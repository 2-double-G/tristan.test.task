import { MenuButton } from '../MenuButton/MenuButton';
import { StyledHeader } from './Header.styles';

export const Header = () => {
  return (
    <StyledHeader>
      <MenuButton />
    </StyledHeader>
  );
};
